true: bin_annot, eliom_ppx

true: \
    package(calendar), \
    package(lwt_ppx), \
    package(lwt_react), \
    package(ppx_compose), \
    package(prime prime.unstable), \
    package(ptime), \
    package(react)

<web/server/*>: \
    thread, \
    package(batyr-core), \
    package(batyr-on-xmpp), \
    package(caqti), \
    package(caqti-lwt), \
    package(caqti-type-calendar), \
    package(config-file), \
    package(eliom.server), \
    package(js_of_ocaml-ppx_deriving_json), \
    package(ppx_deriving), \
    package(ptime.clock.os), \
    package(yojson)

<web/client/*>: \
    package(eliom.client), \
    package(js_of_ocaml-ppx)
